@page
@{
    var languages = Request.Headers["Accept-Language"].ToString();

    if (languages.Contains("uk", StringComparison.OrdinalIgnoreCase))
    {
        Response.Redirect("/uk");
    }
    else
    {
        Response.Redirect("/en");
    }
}